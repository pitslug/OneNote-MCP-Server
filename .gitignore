# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
venv/
.venv/
ENV/
env.bak/
venv.bak/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Auth tokens and sensitive files
.mcp-onenote-cache.json
*.token
*.key
.env
.env.local

# Logs
*.log
logs/

# Local scratch test scripts at repo root (the real suite lives in tests/)
/test_*.py
/*_test.py

# Temporary files
tmp/
temp/
*.tmp
*.temp
claude_desktop_config.json
claude_desktop_config_full.json

# --- Publish hardening (added for public repo) ---
# Runtime secrets (bearer token, client id files) — NEVER commit
secrets/
*_token_cache.json
.onenote_mcp_cache/

# Personal Claude Desktop config (non-example) — keep private
claude_desktop_config.slugworx.json

# Local scratch / debug artifacts
dump_ink.py
inkml_sample.xml
hello*.png
render_out.png
